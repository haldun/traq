<%= simple_form_for(site) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :url %>
    <%= f.input :time_zone %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
